import 'package:flutter/material.dart';

class ButtonSpot extends StatelessWidget {
  const ButtonSpot({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return ElevatedButton(onPressed: () {}, child: Text("2+ spot"));
  }
}