import 'package:flutter/material.dart';

class ButtonMonday extends StatelessWidget {
  const ButtonMonday({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return ElevatedButton(onPressed: () {}, child: Text("Monday"));
  }
}