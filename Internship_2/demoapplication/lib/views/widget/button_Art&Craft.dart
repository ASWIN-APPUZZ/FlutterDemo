import 'package:flutter/material.dart';

class ButtonArt extends StatelessWidget {
  const ButtonArt({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return ElevatedButton(
        onPressed: () {}, child: Text("Art&craft"));
  }
}