import 'package:flutter/material.dart';

class ButtonSuggession extends StatelessWidget {
  const ButtonSuggession({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return ElevatedButton(
        onPressed: () {}, child: const Text("300-980 KO"));
  }
}